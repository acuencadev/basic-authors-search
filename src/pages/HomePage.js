import React from 'react';


export const HomePage = () => {

    return (
        <div className="HomePage">
            <p>Simple application that uses the <a href="https://openlibrary.org/developers/api">OpenLibrary API</a> to display information about authors and books</p>
            <p>Input any query string in the Search bar on top and then hit enter or click the Search button.</p>
        </div>
    );
}
